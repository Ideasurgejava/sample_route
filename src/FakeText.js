import React from 'react';

const FakeText = () => {
  return (
    <div>
      Contact us pages. Everyone has them, needs them, but are you
      really paying attention to the strategy behind them? It’s true
      isn’t it? One of the most valuable pages on your website is
      usually an afterthought. It’s one of those things that has to be
      there, but often enough, you throw the necessary information on
      and leave it at that. What a waste!
    </div>
  );
};

export default FakeText;
